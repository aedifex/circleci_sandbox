# sample config used to demonstrate ssh tunneling
version: 2
jobs:
   build:
     machine: true
     steps:

       - run:
           command: echo "VPN"
           command: sudo apt-get install openvpn

