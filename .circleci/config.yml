version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.6.2
    steps:
      # - checkout
      - run:
          name: HAHA
          command: |
            TEST=123
            echo $TEST
      - run:
          name: Voila
          command: |
            echo $TEST