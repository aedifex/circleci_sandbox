version: 2
jobs:
  build:
    parallelism: 6
    docker:
      - image: circleci/python:latest
    # working_directory: /
    steps:
      # - setup_remote_docker
      - checkout
      - run:
          name: split
          # when: always
          command: |
            # TEST=
            echo "foo"
