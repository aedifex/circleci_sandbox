version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.8
    steps:
      - checkout
      - run: echo "Hello, from build!"
  test:
    docker:
      - image: circleci/golang:1.8
    steps:
      - checkout
      - run: echo "Hello, from test!"
  test-acceptance:
    docker:
      - image: circleci/golang:1.8
    steps:
      - run: echo "Hi, from acceptance!"
  deploy-prod:
    docker:
      - image: circleci/golang:1.8
    steps:
      - run: echo "Hi, from deploy!"

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test:
          requires:
            - build
      - test-acceptance:
          requires:
            - build
      - deploy-prod:
          type: approval
          requires:
            - test
            - test-acceptance