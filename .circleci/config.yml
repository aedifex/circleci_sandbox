version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.8
      - image: elasticsearch:2.4-alpine
    steps:
      - checkout
      - run: echo $CIRCLE_STAGE
  
  test:
    docker:
      - image: circleci/golang:1.8
    steps:
      - checkout
      - run: echo $CIRCLE_STAGE
  
  deploy-staging:
    docker:
      - image: circleci/golang:1.8
    steps:
      - run: echo $CIRCLE_STAGE
  
  deploy-prod:
    docker:
      - image: circleci/golang:1.8
    steps:
      - run: echo $CIRCLE_STAGE

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      #- test:
      #    requires:
      #      - build
      #- deploy-staging:
      #    type: approval
      #    requires:
      #      - test
      #- deploy-prod:
      #    type: approval
      #    filters:
      #      branches:
      #        only: master
      #    requires:
      #      - deploy-staging